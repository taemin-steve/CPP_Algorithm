#include <iostream>
#include <string>
#define PI 3.141592
using namespace std;
string a;

typedef int i; // 타입의 이름을 새롭게 정의 

int main(){
    // i b = 10;
    // cin >> a ;
    // cout << a << b << '\n';
    // cout << PI << '\n';

    // int c, d;
    // scanf("%d . %d", &c, &d); // 다음과 같은 트릭을 이용해서 소수점을 받아내는 것도 가능
    // printf("%d %d", c, d);

    // string s;
    // getline(cin, s); // 개행문자를 같이 입력받고 싶은경우 
    // cout << s << '\n';

    // int T;
    // string s;
    // cin >> T;
    // string bufferflush;
    // getline(cin, bufferflush); // bufferflsuh를 이용하여 cin 뒤에 들어간 개행문자를 받아주어야 한다. 
    // for( int i=0; i< T; i ++){
    //     getline(cin, s);
    //     cout << s << '\n';
    // }

    // double a = 1.23456789;
    // cout << a << '\n';
    // cout.precision(7); // precision을 이용해서 어디까지 보여줄지 선정가능
    // cout << a << '\n';

    // int a = 1, b = 2;
    // printf("이렇게 해서 %d 해서 1 이렇게 %d", a,b);
    // cout << "이렇게 하면 잘만 나오면서" ; 

    // double a = 1.234567;
    // int b = 12;
    // int c = 2;

    // printf("%.2f", a);
    // printf("%02d", b);
    // printf("%02d", c); // .~~ 03 이런식으로 직접 형식을 정해줄 수 있음

    

    return 0;
}